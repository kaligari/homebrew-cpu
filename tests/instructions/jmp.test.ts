import { expect, test } from 'vitest'
import { compile } from '../../src/compiler.js'

test('Test JMP instruction', () => {
    // const program = `
    //     JMP $02
    //     HLT
    // `
    // const compiledProgram = compile(program)
    // expect(compiledProgram).toEqual([0x06, 0x02, 0xFF])
})
